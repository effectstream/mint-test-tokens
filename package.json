{
  "name": "@effectstream/mint-test-tokens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "tsc -b",
    "check": "npm run build && node packages/registry/dist/validate.js",
    "clean": "tsc -b --clean"
  },
  "devDependencies": {
    "@types/node": "24.3.0",
    "ajv": "8.17.1",
    "ajv-formats": "3.0.1",
    "typescript": "5.9.2"
  },
  "engines": { "node": ">=22" }
}
